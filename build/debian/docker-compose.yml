# docker compose -p condabase --verbose up -d --build --force-recreate --remove-orphans
# docker ps -la --no-trunc
# docker compose logs dev
# docker exec -itu "501:20" condabase-dev-1 bash -l
version: "2.4"
services:
  dev:
    environment:
      NEW_USER_NAME: joseph
      NEW_USER_UID: 501
      NEW_USER_GROUP: staff
      NEW_USER_GID: 20
    image: conda_base_dev:v0.0.0a
    build:
      context: .
      dockerfile: Dockerfile
    init: true
    tty: true
    # entrypoint: ["bash", "-lc"]
    command: "tail -f /dev/null" # "dev" # "conda info -a" #
