# docker compose -p conda_base_dev up --build -d
# docker ps -l --no-trunc
# docker compose logs conda-base-dev
version: "2.4"
services:
  conda-base-dev:
    environment:
      NEW_USER_NAME: joseph
      NEW_USER_UID: 501
      NEW_USER_GROUP: staff
      NEW_USER_GID: 20
    image: conda_base_dev:v0.0.0a
    build:
      context: .
      dockerfile: Dockerfile
    init: true
    tty: true
    # entrypoint: ["bash", "-lc"]
    command: "tail -f /dev/null"
